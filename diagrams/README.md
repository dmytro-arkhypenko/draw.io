# Diagrams

Store draw.io (or other) architecture / flow diagrams in this folder.

Suggested conventions:
- Use clear names like `auth-flow.drawio` or `system-overview.drawio`
- Keep exported images next to sources if needed (`.png`, `.svg`)